"""Predictive-maintenance use case (built on the generic ``src.framework`` engine).

The application specifics: the four data sources, their Pydantic schemas and Bronze ORM,
the Silver refinement wiring, the unified Gold feature spec, cross-source analyses and the
orchestrator. Free to import ``src.framework.*`` and the shared kernel (``src.config`` /
``src.quality``).
"""
